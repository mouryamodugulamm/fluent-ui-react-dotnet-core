import { Stack } from "office-ui-fabric-react";
import React from "react";

const Interests = () => {
  return (
    <Stack>
      Currently, no interests are here click add to add interests.
      <button>Add Interests</button>
    </Stack>
  );
};

export default Interests;
